const Container = document.getElementById('container');
Container.classList.add('mt-5', 'pt-1');

export const DisplayPage = document.createElement('div');
DisplayPage.classList.add('my-5');

container.appendChild(DisplayPage);

export default container;